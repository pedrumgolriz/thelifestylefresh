services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: lifestyle
      POSTGRES_PASSWORD: lifestyle
      POSTGRES_DB: lifestylefresh
    ports:
      - "5432:5432"
    volumes:
      - lifestylefresh_pg:/var/lib/postgresql/data

volumes:
  lifestylefresh_pg:
